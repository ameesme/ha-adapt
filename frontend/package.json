{
  "name": "ha-adapt-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web-ui panel for the HA Adapt adaptive lighting integration.",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "lit": "^3.1.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.4.10"
  }
}
